<%= form_for(@orders_j) do |f| %>
  <% if @orders_j.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@orders_j.errors.count, "error") %> prohibited this orders_j from being saved:</h2>

      <ul>
      <% @orders_j.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
